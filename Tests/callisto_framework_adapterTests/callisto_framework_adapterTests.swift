import XCTest
@testable import callisto_framework_adapter

final class callisto_framework_adapterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(callisto_framework_adapter().text, "Hello, World!")
    }
}
